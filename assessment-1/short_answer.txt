1) What does the `rm` command in the terminal do?

Your answer:

The rm commond is used to delete files 
------------------
2) What terminal command would you use to list all the files and folder in the 
   current directory?

Your answer:

I would use the Is command to display the content in files 
------------------
3) Explain the difference between these two commands in the terminal:
   
   $ cd Users
   $ cd /Users

Your answer:
$cd Users : Takes you to the home directory 
cd/ takes you to the root directory

------------------
4) Describe what git is and what it is used for.

Your answer:

Git is a source code management system, it is used to keep track of code changes. Teams us Git to collaborate on code 
------------------
5) Describe what GitHub is and what it is used for.

Your answer:
GitHub is a code hosting platform for version control and collaboration that lets you team together on projects from anywhere.
------------------
6) Which of these commands sets up a new git repository?
    a) git clone 
    b) git pull
    c) git init
    d) git status

Your answer:
c
------------------
7) You want to check that you have properly added all the files you want to be
   tracked in git BEFORE you make a commit. Which command will do this?
     a) git push
     b) git commit
     c) git status
     d) git remote

Your answer:

c
------------------
8) What is the difference between `git add` and `git commit`? 

Your answer:
Git add command adds files to the staging area whereas the git commit command will write chnages to the repository permantly 

------------------
9) What does the `git push` command do?

Your answer:
uploads local repostory content into remote respository 


------------------
10) When would you use the `git clone` command? 

Your answer:
To clone git repository into a specific folder

